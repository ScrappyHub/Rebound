# Packet Constitution v1 — Universal Transport Law (Option A)

This repository is bound to Packet Constitution v1.

Option A (default):
- manifest.json MUST NOT contain packet_id
- packet_id.txt contains PacketId
- sha256sums.txt generated LAST over final on-disk bytes

Verification is non-mutating. Any repair is a separate explicit command with its own artifacts + receipts.

(Authoritative text is maintained by the ecosystem “Universal Transport Law — Packet Constitution v1” spec.)
